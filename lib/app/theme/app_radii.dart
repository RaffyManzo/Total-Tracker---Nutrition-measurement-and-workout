import 'package:flutter/material.dart';

abstract final class AppRadii {
  static const BorderRadius card = BorderRadius.all(Radius.circular(22));
  static const BorderRadius cardLarge = BorderRadius.all(Radius.circular(24));
  static const BorderRadius field = BorderRadius.all(Radius.circular(15));
  static const BorderRadius button = BorderRadius.all(Radius.circular(18));
  static const BorderRadius compact = BorderRadius.all(Radius.circular(13));
  static const BorderRadius pill = BorderRadius.all(Radius.circular(999));
}
