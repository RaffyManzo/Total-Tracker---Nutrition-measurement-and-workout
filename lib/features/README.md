# Features

Future features will be added progressively from the existing Obsidian system.

When useful, each feature can be organized into:

- `data/`
- `domain/`
- `presentation/`

No definitive feature modules are implemented during the initial setup phase.
